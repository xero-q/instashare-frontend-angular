import { ReadableFileSizeDirective } from './readable-file-size.directive';

describe('ReadableFileSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadableFileSizeDirective();
    expect(directive).toBeTruthy();
  });
});
